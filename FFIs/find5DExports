#!/usr/bin/env python

import sets
import sys
def getNames(n):
	result = []
	f = open(n, "r")
	if True:
		for line in f.readlines():
			sys.stdout.write(line)
			if line.find("=") == -1 or not line.startswith("let "):
				continue
			rest = line[len("let ") : ]
			rest = rest[: rest.find("=")].strip()
			name = rest
			result.append(name)
	return(sets.Set(result))

l = ["['%s %s]" % (n, n) for n in getNames(sys.argv[1])]
print "dispatchModule ",
print "[" + ("\n".join(l)) + "]"
