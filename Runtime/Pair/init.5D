let Composition := requireModule "Composition" in 
let fst := Builtins.fst in 
let snd := Builtins.snd in 
let pair? := Builtins.pair? in 
let (,) := Builtins.(,) in 
Composition.dispatch1 (#exports[(,) pair? fst snd])
