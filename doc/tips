(tips4DV1 "The horizontal separator between parameters (λx) and body (x) does not signify a fraction"
          "You can complete text by pressing the Tabulator (Tab) key on your keyboard"
          "You can define new functionals by (define sinc \\x (sin x)/x)"
          "You can chain I/O actions by appending the character ';', followed by a continuation functional. (a;\\input b)"
          "You can add a comment by (rem \"comment\" <body>) where <body> is the non-comment part"
)